<main class='container mt-4'>
  <div class='row'>
    <div class='col-6'>
      <h1 class='mb-3'>Yeez-n-Jays Dashboard</h1>
      <a href='/' class='btn btn-primary'>Show Me Yeez</a>
      <a href='/' class='btn btn-primary'>Show Me Jays</a>
    </div>
  </div>
  <br />
  <h1 class='mb-3'>Shop Yeez-n-Jays</h1>
  {{! Gabe: make this look pretty!
   Be sure to reference the columns from Product.js to get
   whatever information you need!}}
  {{#each shoes as |shoe|}};
    <div class='card'>
      <div class='card-header'>
        <h2>{{shoe.product_name}}</h2>
        <h3>${{shoe.price}}</h3>
      </div>
    </div>
  {{/each}}
</main>